{% macro backLink(params) %}

  {%- from "icons/_macro.njk" import icon -%}

  <a class="back-link{{ ' ' + params.classes if params.classes else '' }}" href="{{ params.url }}">
    {{
      icon({
        "icon": 'chevron-left'
      })
    }}
    {{ params.text }}
  </a>
{% endmacro %}